services:
  - type: web
    name: practiceCAD
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: cd server && npm start
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
